<#
    Create public/private key
    -C => Command {
        Create -> Create public and private key
        CreatePublic -> Create public key
        CreatePrivate -> Create private key
    }
#>
param
(
    [string]$C = ""
)
if($C = ""){
    
}